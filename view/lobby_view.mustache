{{>header}}
<div class="flex flex-col justify-center items-center justify-items-center">
 <div class="lobby-container mt-10">
    <div class="lobby-user">
        {{#usuarioInfo}}
            <h1 class="text-white text-3xl mb-20 uppercase">{{nickName}}</h1>
        {{/usuarioInfo}}
        {{#usuarioJuego}}
            <h2 class="text-white text-3xl mb-20">{{puntaje}} Puntos</h2>
        {{/usuarioJuego}}
        {{#usuarioInfo}}
            <a href="/user/mostrar" ><img src="/public/foto-perfil/{{imagenPerfil}}" class="w-16 h-16 rounded-full border border-yellow-400"></a>
        {{/usuarioInfo}}
    </div>
    <div class="linea"></div>
    <div class="lobby-buttons">
        <a href="/partida/list" class="boton bg-yellow-400 hover:bg-yellow-200 text-black p-5 rounded">Jugar Partida</a>
    </div>
    {{#usuarioJuego}}
        <h2 class="text-white text-3xl mb-20">{{trampita}} Trampitas</h2>
    {{/usuarioJuego}}
</div>
</div>
{{>footer}}